apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-curso-dio
  namespace: default
  labels:
    app: pvc-curso-dio
spec:
  storageClassName: standard-rwo
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi